---
domain: http://localhost:8000

urls:
  - /

fetch_options:
  cache_enabled: false

entity_type: phpunit_test

mappings:
  -
    field: alias
    type: alias
  -
    field: test_result
    selector: '//h1'
    type: text
    processors:
      whitespace: { }